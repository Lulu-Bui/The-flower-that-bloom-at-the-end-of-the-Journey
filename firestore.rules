rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through the backend (Cloud Functions),
    // so we lock down direct client access entirely.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
